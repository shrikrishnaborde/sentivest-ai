# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Database ---
sqlalchemy==2.0.35
asyncpg==0.29.0
alembic==1.13.2

# --- RAG / LLM ---
openai==1.51.0
# Ingestion's Anthropic research fallback (web_search tool, output_config.effort)
# needs a current SDK — pin a floor rather than an exact version.
anthropic>=0.70.0,<1.0.0
chromadb==0.5.11
pinecone-client==5.0.1
qdrant-client==1.11.3

# --- Sentiment (FinBERT) ---
transformers==4.45.1
torch==2.4.1
sentencepiece==0.2.0

# --- Ingestion ---
httpx==0.27.2
celery==5.4.0
redis==5.0.8

# --- Utilities ---
python-dotenv==1.0.1
tenacity==9.0.0
